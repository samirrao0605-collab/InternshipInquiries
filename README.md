# Internship Inquiries API

A Spring Boot REST API project using PostgreSQL to manage internship leads.

## Features
- Add new internship leads
- View all leads
- Database integration with PostgreSQL
- Tested with Postman

## Setup
1. Clone the repo
2. Run `mvn spring-boot:run`
3. Access API at `http://localhost:8080/api/leads`
